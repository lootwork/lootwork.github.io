// config.js — настройки сайта. Единственный файл, который ты правишь руками.
// После правки просто сохрани и обнови страницу в браузере.

window.CONFIG = {

  // ------------------------------------------------------------------
  // 1. ФОРМА ЖАЛОБ (кнопка «неактуальна» на карточке)
  //
  // Ответы падают в Google Форму «LOOTWORK — жалобы».
  // url — адрес приёма ответов, fields — номера полей формы.
  // Отправка идёт в фоне: человек не уходит с сайта.
  // ------------------------------------------------------------------
  reportForm: {
    url: "https://docs.google.com/forms/d/e/1FAIpQLSeKRgzzSjJiAyYDO_x9BCHrb8j33-3kx5aJiZq_IU2s-A4I0g/formResponse",
    fields: {
      company: "entry.1464706843",   // Студия
      title:   "entry.956125046",    // Вакансия
      url:     "entry.492084555",    // Ссылка
      reason:  "entry.1288465243",   // Причина
      when:    "entry.581931613",    // Когда
      id:      "entry.1212083325"    // id — техническое, в конце
    }
  },

  // ------------------------------------------------------------------
  // 2. ФОРМА ОБРАТНОЙ СВЯЗИ (кнопка «Написать нам»)
  //
  // Форма «LOOTWORK — обратная связь».
  // ------------------------------------------------------------------
  feedbackForm: {
    url: "https://docs.google.com/forms/d/e/1FAIpQLSeiV24bz2xNDw64yu4MLETIO1pAGwS6NN0sbi8ECkThb6T52Q/formResponse",
    fields: {
      topic:   "entry.1900534920",   // Тема
      text:    "entry.1820856742",   // Сообщение
      contact: "entry.324914555"     // Контакт для ответа
    }
  },

  // ------------------------------------------------------------------
  // 3. ЯНДЕКС.МЕТРИКА (посещаемость и время на сайте)
  // ------------------------------------------------------------------
  metrikaId: 111770236,

  // ------------------------------------------------------------------
  // 4. Сколько жалоб нужно, чтобы вакансия скрылась у всех.
  // Пока считается только у тебя в таблице — это на будущее.
  // ------------------------------------------------------------------
  reportsToHide: 3
};
