// config.js — настройки сайта. Единственный файл, который ты правишь руками.
// После правки просто сохрани и обнови страницу в браузере.

window.CONFIG = {

  // ------------------------------------------------------------------
  // 1. ФОРМА ЖАЛОБ (Google Формы)
  //
  // Сюда вставь ссылку на предзаполненную форму. Как её получить —
  // пошагово написано в файле НАСТРОЙКА.md
  //
  // В ссылке оставь метки {id}, {title}, {reason}, {when} — сайт
  // подставит в них данные автоматически.
  //
  // Пока тут null, кнопка «неактуальна» работает, но ответы никуда
  // не уходят — просто прячет вакансию у того, кто нажал.
  // ------------------------------------------------------------------
  reportForm: null,

  // ------------------------------------------------------------------
  // 2. ФОРМА ОБРАТНОЙ СВЯЗИ (кнопка «Написать нам»)
  //
  // Делается так же, как форма жалоб. Метки: {topic} и {text}.
  // ------------------------------------------------------------------
  feedbackForm: null,

  // ------------------------------------------------------------------
  // 3. ЯНДЕКС.МЕТРИКА (посещаемость и время на сайте)
  //
  // Вставь номер счётчика, например: 98765432
  // Где взять — тоже в НАСТРОЙКА.md
  // ------------------------------------------------------------------
  metrikaId: 111770236,

  // ------------------------------------------------------------------
  // 4. Сколько жалоб нужно, чтобы вакансия скрылась у всех.
  // Пока считается только у тебя в таблице — это на будущее.
  // ------------------------------------------------------------------
  reportsToHide: 3
};
